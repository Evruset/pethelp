# Deprecated path

Целевая версия продукта — V50. Программные документы перенесены в `docs/v50/`. V51 была указана ошибочно и не существует.

`design-tokens.json` временно остаётся здесь как единственный compatibility-файл, потому что существующий read-only portal test импортирует этот путь. План безопасного переноса записан в `docs/v50/V50-NAMING-DEBT.md`; дубликат файла не создаётся.
