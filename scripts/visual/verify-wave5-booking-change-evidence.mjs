import {createHash} from 'node:crypto';import {readFile,readdir} from 'node:fs/promises';import {resolve} from 'node:path';
const root=resolve(import.meta.dirname,'../..'),dir=resolve(root,'docs/testing/evidence/wave5-booking-change-closure'),manifest=JSON.parse(await readFile(resolve(dir,'manifest.json'),'utf8'));const required=['owner-booking-before-change','owner-cancel-confirmation','owner-cancel-request-open','ops-cancel-queue','ops-cancel-detail','owner-booking-cancelled','owner-reschedule-confirmation','owner-reschedule-request-open','ops-reschedule-queue','ops-replacement-slot-selection','ops-reschedule-completed','owner-booking-rescheduled','stale-replacement-conflict'];const sha=value=>createHash('sha256').update(value).digest('hex');if(manifest.status!=='PASS'||manifest.physicalDevice!==false)throw new Error('manifest status/classification mismatch');for(const gate of ['realCancel','realReschedule','staleSafety','ownerReadback','operationsReadback','capacityFinalState'])if(manifest.checks[gate]!=='PASS')throw new Error(`failed gate ${gate}`);if(manifest.checks.axeSeriousCritical!==0||manifest.checks.horizontalOverflow!==0||manifest.checks.minActionHeight<44||manifest.checks.keyboardFocus.status!=='PASS')throw new Error('accessibility gate mismatch');if(manifest.cases.length<18||manifest.cases.length>24)throw new Error('screenshot bound mismatch');for(const state of required)if(!manifest.states.includes(state))throw new Error(`missing state ${state}`);for(const viewport of ['390x844','768x1024','1440x900','430x932','1024x768'])if(!manifest.viewports.includes(viewport))throw new Error(`missing viewport ${viewport}`);for(const [file,hash] of Object.entries(manifest.sources))if(sha(await readFile(resolve(root,file)))!==hash)throw new Error(`source hash mismatch ${file}`);for(const item of manifest.cases)if(sha(await readFile(resolve(dir,item.file)))!==item.sha256)throw new Error(`image hash mismatch ${item.file}`);const pngs=(await readdir(dir)).filter(file=>file.endsWith('.png')).sort(),listed=manifest.cases.map(item=>item.file).sort();if(JSON.stringify(pngs)!==JSON.stringify(listed))throw new Error('orphan or missing screenshot');console.log(`Wave 5 booking change evidence verified: ${listed.length}/${listed.length}`);
