name: Deploy Immutable Alpha Artifact

on:
  workflow_run:
    workflows: ['Backend CI and Immutable Release']
    branches: [main]
    types: [completed]
  workflow_dispatch:
    inputs:
      artifact_run_id:
        description: 'Successful Backend CI run ID containing vethelp-alpha-immutable-manifest'
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: vethelp-alpha-deploy
  cancel-in-progress: false

jobs:
  deploy:
    if: >-
      (github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success') &&
      vars.VETHELP_ALPHA_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    environment: alpha
    steps:
      - uses: actions/checkout@v4
      - name: Resolve source run
        id: source
        run: |
          echo "run_id=${{ inputs.artifact_run_id || github.event.workflow_run.id }}" >> "$GITHUB_OUTPUT"
      - uses: actions/download-artifact@v4
        with:
          name: vethelp-alpha-immutable-manifest
          run-id: ${{ steps.source.outputs.run_id }}
          github-token: ${{ github.token }}
          path: artifacts/release
      - uses: azure/setup-kubectl@v4
      - name: Configure Kubernetes client
        env:
          KUBECONFIG_DATA: ${{ secrets.VETHELP_ALPHA_KUBECONFIG }}
        run: |
          set -euo pipefail
          test -n "$KUBECONFIG_DATA"
          echo "$KUBECONFIG_DATA" | base64 --decode > "$RUNNER_TEMP/kubeconfig"
          chmod 600 "$RUNNER_TEMP/kubeconfig"
          echo "KUBECONFIG=$RUNNER_TEMP/kubeconfig" >> "$GITHUB_ENV"
      - name: Apply immutable release
        run: |
          set -euo pipefail
          grep -Eq 'image: .+@sha256:[0-9a-f]{64}$' artifacts/release/vethelp-alpha.yaml
          kubectl apply -f artifacts/release/vethelp-alpha.yaml
      - name: Validate Alpha prerequisites and rollout
        env:
          DEPLOYMENT_NAME: alpha-vethelp-backend
          EXTERNAL_SECRET_NAME: alpha-vethelp-backend-runtime
          RUNTIME_SECRET_NAME: alpha-vethelp-backend-secret
          PUBLIC_INGRESS_NAME: alpha-vethelp-public
          MIS_INGRESS_NAME: alpha-vethelp-mis-mtls
          NETWORK_POLICY_NAME: alpha-vethelp-backend-restricted
        run: bash scripts/alpha-preflight.sh
