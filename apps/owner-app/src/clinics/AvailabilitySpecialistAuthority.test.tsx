import {act,fireEvent,render,waitFor} from '@testing-library/react-native';
import {AvailabilityScreen} from './AvailabilityScreen';
const mockC='11111111-1111-4111-8111-111111111111',mockL='22222222-2222-4222-8222-222222222222',mockS='33333333-3333-4333-8333-333333333333',mockT='44444444-4444-4444-8444-444444444444';
const mockContext={clinicId:mockC,locationId:mockL,serviceId:mockS},mockSlot={slotId:mockT,startsAt:'2026-09-10T08:00:00.000Z',endsAt:'2026-09-10T08:30:00.000Z',localDate:'2026-09-10',localTime:'11:00',expectedVersion:2},mockData={observedAt:'2026-09-08T08:00:00.000Z',clinicName:'Clinic',serviceName:'Осмотр',timezone:'Europe/Moscow',horizonEndsAt:'2026-09-22T08:00:00.000Z',slots:[mockSlot]};
jest.mock('@tanstack/react-query',()=>({useQuery:()=>({isPending:false,isError:false,data:mockData,refetch:jest.fn().mockResolvedValue({isError:false,data:mockData})})}));
jest.mock('@/session/SessionProvider',()=>({useSession:()=>({session:{cacheScope:'owner',opaqueCredential:'token'}})}));
it('ignores specialist revalidation completed after authority replacement',async()=>{let resolve!:(value:boolean)=>void;const deferred=new Promise<boolean>(value=>{resolve=value;}),onContinue=jest.fn();const screen=await render(<AvailabilityScreen authorityGeneration="session-a" context={mockContext} preferredSlot={{slotId:mockT,expectedVersion:2}} revalidateContext={()=>deferred} onBack={jest.fn()} onContinue={onContinue}/>);await waitFor(()=>expect(screen.getByRole('radio').props.accessibilityState.selected).toBe(true));fireEvent.press(screen.getByText('Продолжить'));await act(async()=>screen.rerender(<AvailabilityScreen authorityGeneration="session-b" context={mockContext} preferredSlot={{slotId:mockT,expectedVersion:2}} revalidateContext={()=>deferred} onBack={jest.fn()} onContinue={onContinue}/>));await act(async()=>{resolve(true);await deferred;});expect(onContinue).not.toHaveBeenCalled();});
