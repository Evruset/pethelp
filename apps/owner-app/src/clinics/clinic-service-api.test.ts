import { createClinicServiceApi, parseClinicServiceSnapshot } from './clinic-service-api';
const C='11111111-1111-4111-8111-111111111111',L='22222222-2222-4222-8222-222222222222',S='33333333-3333-4333-8333-333333333333',D='44444444-4444-4444-8444-444444444444';
const specialist={doctorId:D,displayName:'Анна Иванова',specialtyName:'Терапевт',nextAvailability:{startsAt:'2026-08-14T08:00:00.000Z',localDate:'2026-08-14',localTime:'11:00',timezone:'Europe/Moscow'}};
const payload={observedAt:'2026-08-13T08:00:00.000Z',clinicId:C,locationId:L,name:'Clinic',address:'Address',phone:null,services:[{serviceId:S,name:'Осмотр',price:{kind:'INFORMATIONAL',amount:'1250.00',currency:'RUB'},specialty:null,specialists:[specialist]}]};
describe('clinic service api',()=>{
  it('uses the scoped route and accepts only the public projection',async()=>{const request=jest.fn().mockResolvedValue(payload);await expect(createClinicServiceApi({request}).read('credential',C,L)).resolves.toEqual(payload);expect(request).toHaveBeenCalledWith(`v1/owner/clinic-catalog/${C}/locations/${L}`,expect.objectContaining({headers:{Authorization:'Bearer credential'}}));});
  it.each([{...payload,ownerId:C},{...payload,services:[{...payload.services[0],paymentStatus:'PAID'}]},{...payload,services:[{...payload.services[0],price:{...payload.services[0].price,amount:'12'}}]},{...payload,services:[{...payload.services[0],specialists:[{...specialist,email:'doctor@example.test'}]}]},{...payload,services:[{...payload.services[0],specialists:[{...specialist,displayName:D}]}]},{...payload,services:[{...payload.services[0],specialists:[{...specialist,displayName:` ${D} `}]}]},{...payload,services:[{...payload.services[0],specialists:[{...specialist,nextAvailability:null}]}]},{...payload,services:[{...payload.services[0],specialists:Array.from({length:11},()=>specialist)}]},{...payload,services:Array.from({length:51},()=>payload.services[0])}])('fails closed for malformed, unsafe, unbounded, or extra fields',value=>expect(()=>parseClinicServiceSnapshot(value)).toThrow('INVALID_CLINIC_SERVICE_RESPONSE'));
});
